-- ОТКАТ ограничений миграции 0007 (пределы одновременно активных операций).
--
-- ЭТОТ ФАЙЛ НАМЕРЕННО ЛЕЖИТ ВНЕ `drizzle/`.
-- Раннер миграций применяет ВСЁ содержимое `drizzle/*.sql` автоматически при
-- старте контейнера (docker-entrypoint.sh). Файл, положенный туда «на всякий
-- случай», снял бы ограничения на ближайшем же деплое — молча и в проде.
-- Копировать в `drizzle/` только в момент осознанного отката, см. docs/rollback.md.
--
-- Что делает: возвращает поведение до 0007 — у врача снова может быть несколько
-- незакрытых операций, и их общее число ничем не ограничено (§8.5 в прежнем виде).
--
-- Чего НЕ делает: не трогает данные. Справочник врачей, коды операций, снимки
-- врача и колонки из 0006 остаются на месте — старому коду они не мешают, он их
-- просто не читает. Ни одна операция, ни одно движение остатка не изменяются.

DROP TRIGGER IF EXISTS trg_operations_room_limit_insert;
DROP TRIGGER IF EXISTS trg_operations_room_limit_update;
DROP INDEX IF EXISTS ux_operations_single_active_per_doctor;
