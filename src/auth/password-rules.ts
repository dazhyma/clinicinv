/**
 * Правила, безопасные для импорта в клиентский код.
 *
 * Само хеширование остаётся в `password.ts` и никогда не попадает в браузерный
 * bundle вместе с нативным модулем Argon2.
 */
export const MIN_PASSWORD_LENGTH = 10;
